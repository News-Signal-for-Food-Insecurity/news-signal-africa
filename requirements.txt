# Python >= 3.9 required (uses list[str] type hints and match expressions)
# Tested on Python 3.10 and 3.11

# Core ML
catboost>=1.2
scikit-learn>=1.3

# Data
numpy>=1.24
pandas>=2.0
pyarrow>=12.0

# Visualisation
matplotlib>=3.7
seaborn>=0.12
scipy>=1.10

# Parallelism (null shuffle test)
joblib>=1.3

# Geospatial (Fig 1 choropleth maps only — optional)
# If not installed, Fig 1 is skipped; all other figures still generate.
geopandas>=0.14
shapely>=2.0
